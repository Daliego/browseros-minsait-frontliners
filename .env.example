# BrowserOS — Environment Variables
# Copy this file to apps/shell/.env.local and set the values.

# URLs for microfrontend apps (used by the shell to open iframes)
NEXT_PUBLIC_DEPLOY_LIST_APP_URL=http://localhost:3001
NEXT_PUBLIC_DEPLOY_RUNNER_APP_URL=http://localhost:3002
